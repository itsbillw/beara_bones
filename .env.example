# Copy to .env and fill in values. Never commit .env.

# Required for both dev and production (use a long random string in production)
DJANGO_SECRET_KEY=your-secret-key-here

# Production only: comma-separated host names (e.g. itsbillw.eu,www.itsbillw.eu)
# ALLOWED_HOSTS=itsbillw.eu,www.itsbillw.eu

# Production only: MariaDB on DietPi / Raspberry Pi
# DB_NAME=beara_bones
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=127.0.0.1
# DB_PORT=3306
